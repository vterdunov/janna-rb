---
sudo: required

language: ruby

services:
  - docker

before_install:
  - docker build -t vterdunov/janna .
  - docker run vterdunov/janna /bin/sh -c 'bundle exec rspec'

script:
  - bundle exec rspec
